{
  "name": "atlas",
  "version": "0.1.0",
  "private": true,
  "description": "Atlas — agentic QA pipeline: provision an ephemeral environment, seed mock data, run hybrid deterministic + AI-agent test flows, emit a report.",
  "type": "module",
  "bin": {
    "atlas": "src/cli.ts"
  },
  "scripts": {
    "atlas": "tsx src/cli.ts"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.3.0",
    "playwright": "^1.48.0",
    "yaml": "^2.5.0",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "@types/node": "^22.0.0"
  }
}
